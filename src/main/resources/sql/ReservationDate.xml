<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kirin.linebot.repository.mapper.ReservationDateMapper">
    <select id="insert">
        INSERT INTO RESERVATION_DATE
          (ID, NAME, DATE, CREATEDA_AT, UPDATED_AT) 
        VALUES 
          (null, #{targetName}, #{reservationDate}, SYSDATE(), SYSDATE())
    </select>
</mapper>